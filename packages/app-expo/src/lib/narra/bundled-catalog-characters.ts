import type { NarraCharacter } from "./types";

/** Backend catalog manifests are the only source of catalog characters. */
export function getBundledCatalogCharactersByTitle(_title: string): NarraCharacter[] | undefined {
  return undefined;
}

/** Backend catalog manifests are the only source of catalog characters. */
export function getBundledCatalogCharactersById(_id: string): NarraCharacter[] | undefined {
  return undefined;
}
